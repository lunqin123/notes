# 终端美化优化合集

> Windows 终端 + Claude Code 全面美化优化指南

---

## 目录

- [一、Windows 终端半透明化](#一windows-终端半透明化)
- [二、Claude Code HUD 状态栏](#二claude-code-hud-状态栏)
- [三、费用显示（人民币 ¥）](#三费用显示人民币-)
- [四、缓存命中率监控](#四缓存命中率监控)
- [五、DeepSeek 定价支持与自动更新](#五deepseek-定价支持与自动更新)
- [六、Claude Code 完整配置](#六claude-code-完整配置)
- [七、文件结构总览](#七文件结构总览)

---

## 一、Windows 终端半透明化

### 1.1 Windows Terminal（推荐）

Windows Terminal 原生支持亚克力/半透明效果，设置最方便。

#### 方法一：GUI 设置
1. 打开 Windows Terminal
2. 点击标题栏下拉箭头 → **设置**（或按 `Ctrl + ,`）
3. 左侧选择 **配置文件** → **默认值**（全局生效）或选择特定终端（如 PowerShell）
4. 点击 **外观** 选项卡
5. 找到 **背景图像** → **背景不透明度**，拖动滑块调节（建议 60%-85%）
6. 启用 **启用亚克力材料** 开关，获得毛玻璃模糊效果
7. 点击 **保存**

#### 方法二：JSON 配置文件
打开设置 → 左下角 **打开 JSON 文件**，在 `profiles.defaults` 中添加：

```json
"profiles": {
    "defaults": {
        "opacity": 70,
        "useAcrylic": true
    }
}
```

- `opacity`: 0-100，数值越小越透明（建议 60-85）
- `useAcrylic`: 启用亚克力毛玻璃效果（Win10 1809+ 支持）

#### 针对特定终端配置示例
```json
"profiles": {
    "list": [
        {
            "guid": "{...}",
            "name": "PowerShell",
            "opacity": 75,
            "useAcrylic": true
        },
        {
            "guid": "{...}",
            "name": "Ubuntu",
            "opacity": 70,
            "useAcrylic": true
        }
    ]
}
```

### 1.2 传统控制台（CMD / PowerShell 5）

适用于未使用 Windows Terminal 的默认宿主：

1. 右键点击标题栏 → **属性**
2. 切换到 **终端** 选项卡
3. 找到 **透明度** 区段
4. 勾选 **启用透明度**，调节滑块

> 注意：此功能仅 Windows 11 / Windows 10 build 19041+ 支持。

### 1.3 VS Code 内嵌终端

在 VS Code `settings.json` 中添加：

```json
"terminal.integrated.windowsEnableAcrylic": true,
"terminal.integrated.opacity": 0.7
```

- `opacity`: 0-1 之间的小数

### 1.4 第三方终端

| 终端 | 设置方式 |
|------|----------|
| **WezTerm** | `config.lua` 中设置 `window_background_opacity = 0.8` |
| **Alacritty** | `alacritty.toml` 中设置 `[window] opacity = 0.8` |
| **Hyper** | 安装插件 `hyper-transparent`，或设置 CSS 覆盖 |
| **MobaXterm** | Settings → Display → Transparency |

### 1.5 命令行热键调节（Windows Terminal）

`Ctrl + Shift + 滚轮上/下` → 调节当前终端的透明度（Windows Terminal 默认支持）

#### 推荐组合

- **Windows Terminal** + `opacity: 75` + `useAcrylic: true`
- 背景选深色主题（如 One Half Dark），半透明效果最佳
- 如果感觉文字不清晰，可以适当提高 opacity（如 80-85），或关闭亚克力仅用透明度

---

## 二、Claude Code HUD 状态栏

自定义 HUD 插件替代 Claude Code 默认状态栏，多行显示实时信息。

### 2.1 安装

claude-hud 插件通过 Claude Code 插件市场安装：

```
/claude-hud:setup
```

### 2.2 显示效果（expanded 模式）

```
[deepseek-v4-flash] │ ~/Desktop/Claude_projects git:(main)
上下文 ██░░░░░░░░ 24% │ 用量 █░░░░░░░░░ 7% (5h)
缓存命中 0.000% │ 估算 ¥0.05
```

- **第一行：** 模型名、项目路径、Git 分支
- **第二行：** 上下文进度条、用量配额（5h/7d 百分比）
- **第三行：** 缓存命中率、费用估算

### 2.3 HUD 配置

**位置：** `~/.claude/plugins/claude-hud/config.json`

```json
{
  "language": "en",
  "lineLayout": "expanded",
  "display": {
    "showTools": false,
    "showAgents": false,
    "showProject": true,
    "showConfigCounts": true,
    "showSpeed": true,
    "showUsage": true,
    "showDuration": true,
    "showSessionName": true,
    "showSessionTokens": true,
    "showCost": true
  }
}
```

### 2.4 核心配置项说明

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `showCost` | `false` | 显示费用估算（需设为 `true`） |
| `costCurrency` | `'cny'` | 货币：`'usd'` / `'cny'` / `'both'` |
| `showCacheHitRate` | `true` | 显示缓存命中率 |
| `showTools` | `false` | 工具活动行（Edit/Bash 等） |
| `showAgents` | `false` | 子代理活动行 |
| `lineLayout` | `'expanded'` | 布局：`'expanded'` / `'compact'` |

---

## 三、费用显示（人民币 ¥）

### 3.1 特性

- 默认以 **人民币（¥）** 显示，支持三种模式
- 汇率：1 USD ≈ 7.2 CNY
- 费用值用 **青色（cyan）** 高亮显示

### 3.2 货币模式

| `costCurrency` | 显示示例 |
|----------------|----------|
| `'cny'`（默认） | `估算 ¥0.05` |
| `'usd'` | `Est. $0.01` |
| `'both'` | `估算 ¥0.05 ($0.01)` |

### 3.3 金额格式化规则

| 金额范围 | 格式 | 示例 |
|----------|------|------|
| ≥ ¥1 | 2 位小数 | `¥1.23` |
| ≥ ¥0.01 | 2 位小数 | `¥0.05` |
| ≥ ¥0.0001 | 4 位小数 | `¥0.0050` |
| < ¥0.0001 | 显示最小值 | `< ¥0.0001` |

---

## 四、缓存命中率监控

### 4.1 计算公式

```
缓存命中率 = cacheReadTokens ÷ (inputTokens + cacheCreationTokens + cacheReadTokens) × 100%
```

输出 token 不计入分母（输出不会被缓存）。

### 4.2 显示

- 显示 3 位小数，如 `缓存命中 75.123%`
- 颜色随命中率变化（绿/黄/红），和上下文进度条一致
- 会话累计统计，每次 API 调用后更新

---

## 五、国产模型定价支持

### 5.1 支持的模型与价格

HUD 内置 **8 家厂商、18 款主流国产模型**的定价，自动识别模型名称并正确计费。

#### DeepSeek

| 模型 | 输入（/M tokens） | 输出（/M tokens） | 缓存命中（/M tokens） |
|------|------------------|------------------|--------------------|
| V4 Flash | ¥1 | ¥2 | ¥0.02 |
| V4 Pro (2.5折) | ¥3 | ¥6 | ¥0.025 |

#### 通义千问（阿里巴巴）

| 模型 | 输入（/M tokens） | 输出（/M tokens） | 缓存命中（/M tokens） |
|------|------------------|------------------|--------------------|
| Qwen-Turbo | ¥0.5 | ¥1 | ¥0.05 |
| Qwen-Plus | ¥2.2 | ¥6.6 | ¥0.22 |
| Qwen-Max | ¥8 | ¥24 | ¥0.8 |

#### 智谱 GLM

| 模型 | 输入（/M tokens） | 输出（/M tokens） | 缓存命中（/M tokens） |
|------|------------------|------------------|--------------------|
| GLM-4-Flash | ¥0.5 | ¥1 | ¥0.05 |
| GLM-4 | ¥4.8 | ¥14.4 | ¥0.48 |
| GLM-5 | ¥4 | ¥12 | ¥0.4 |

#### 其他模型

| 厂商 | 模型 | 输入（/M tokens） | 输出（/M tokens） |
|------|------|------------------|------------------|
| 月之暗面 | Kimi-Small | ¥1 | ¥2 |
| 月之暗面 | Kimi K2.5 | ¥6 | ¥12 |
| 百川智能 | Baichuan3-Turbo | ¥12 | ¥12 |
| 零一万物 | Yi-Medium | ¥2.5 | ¥2.5 |
| 零一万物 | Yi-Large | ¥20 | ¥20 |
| MiniMax | MiniMax-Lite | ¥0.8 | ¥1.5 |
| MiniMax | MiniMax M2.5 | ¥2.2 | ¥17.5 |
| 阶跃星辰 | Step 3.5 Flash | ¥0.73 | ¥2.19 |
| 阶跃星辰 | Step-3 | ¥1.54 | ¥4.16 |

> 缓存命中价格默认按输入价的 10% 估算（DeepSeek 除外）。各厂商官网价格可能随时调整，请以官方最新公告为准。

### 5.2 缓存价格乘数

| 模型 | 缓存读取（占输入%） | 缓存写入（占输入%） |
|------|-------------------|-------------------|
| Anthropic 系列 | 10% | 125% |
| DeepSeek V4 Flash | **2%** | **100%（不额外收费）** |
| DeepSeek V4 Pro | **0.83%** | **100%（不额外收费）** |
| 其他国产模型 | 10%（默认估算） | 100%（不额外收费） |

### 5.3 自动定价更新机制

HUD 自动从 DeepSeek 官网同步最新定价：

```
┌─────────────┐    缓存过期(24h)    ┌──────────────┐    HTTPS GET    ┌──────────────────┐
│  HUD 渲染    │ ─────────────────→ │ pricing-cache │ ────────────→ │ DeepSeek 官网     │
│  (~300ms/次) │                    │ (文件缓存)     │               │ /pricing/         │
└─────────────┘                    └──────────────┘               └──────────────────┘
       ↑                                │                                │
       │ 读缓存                          │ 写缓存                         │ HTML 解析
       │ 或硬编码回退                      │                                ↓
       └────────────────────────────────┘                     ┌──────────────────┐
                                                                │ pricing-fetcher  │
                                                                │ (后台子进程)      │
                                                                └──────────────────┘
```

- **缓存文件：** `~/.claude/plugins/claude-hud/pricing-cache/pricing-cache.json`
- **有效期：** 24 小时
- **防并发：** 文件锁 + 5 分钟冷却
- **降级：** 官网不可用时自动回退硬编码默认定价

### 5.4 价格变化通知

定价拉取后自动与上次缓存比对，**有变化时即时通知**：

```
估算 ¥0.05 📢 v4-flash 输入: ¥1→¥2 ↑100%
```

- **检测范围：** 输入价、输出价、缓存命中价，任一变化即触发
- **通知时效：** 变化后 **1 小时内**在 HUD 显示，之后自动消失
- **显示位置：** 附在费用行末尾，黄色高亮
- **即使关闭 `showCost`**，价格变化通知依然可见

---

## 六、Claude Code 完整配置

**位置：** `~/.claude/settings.json`

```json
{
  "env": {
    "ANTHROPIC_BASE_URL": "https://api.deepseek.com/anthropic",
    "ANTHROPIC_AUTH_TOKEN": "sk-...",
    "CLAUDE_CODE_EFFORT_LEVEL": "max",
    "ANTHROPIC_MODEL": "deepseek-v4-flash[1m]"
  },
  "language": "中文",
  "model": "opus",
  "statusLine": {
    "type": "command",
    "command": "<claude-hud 启动命令>"
  },
  "hooks": {
    "Stop": [{ "hooks": [{ "type": "command", "command": "播放提示音", "async": true }] }],
    "PermissionRequest": [{ "hooks": [{ "type": "command", "command": "播放提示音", "async": true }] }]
  }
}
```

### 配置项说明

| 配置 | 说明 |
|------|------|
| `ANTHROPIC_BASE_URL` | API 代理地址（指向 DeepSeek Anthropic 兼容接口） |
| `ANTHROPIC_MODEL` | 使用的模型 ID |
| `CLAUDE_CODE_EFFORT_LEVEL` | 推理 effort 级别（`max` 最详尽） |
| `language` | 交互语言（`中文` 或 `en`） |
| `model` | Claude Code 内部模型选择 |
| `statusLine` | 自定义状态栏命令（加载 HUD 插件） |
| `hooks.Stop` | 会话结束时播放提示音 |
| `hooks.PermissionRequest` | 权限请求时播放提示音 |

---

## 七、文件结构总览

```
~/.claude/
├── settings.json                          # 主配置文件
├── settings.local.json                    # 本地覆盖配置
├── sounds/
│   ├── done.wav                           # 会话结束提示音
│   └── prompt.wav                         # 权限请求提示音
├── keybindings.json                       # 快捷键绑定
└── plugins/
    ├── claude-hud/
    │   └── config.json                    # HUD 插件配置
    └── cache/claude-hud/claude-hud/0.1.0/
        ├── src/
        │   ├── cost.ts                    # 费用计算（含 CNY 支持）
        │   ├── pricing-cache.ts           # 定价缓存管理
        │   ├── pricing-fetcher.ts         # 后台定价拉取
        │   ├── stdin.ts                   # stdin 数据解析
        │   ├── transcript.ts              # 会话转录解析
        │   └── render/lines/
        │       ├── cost.ts                # 费用展示（高亮）
        │       ├── cache-hit-rate.ts      # 缓存命中率展示
        │       ├── identity.ts            # 上下文进度条
        │       └── usage.ts               # 用量配额显示
        ├── dist/                          # TypeScript 编译产物
        └── config.js                      # 插件默认配置
```

---

> **GitHub 仓库：** [lunqin123/notes](https://github.com/lunqin123/notes)
>
> *持续更新中…*
